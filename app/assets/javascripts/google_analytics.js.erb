document.addEventListener('turbolinks:load', function(event) {
  if (typeof gtag === 'function') {
    gtag('config', '<%= ENV.fetch("GOOGLE-TRACKING_ID") %>', {
      'page_location': event.data.url
    })
  }
})
