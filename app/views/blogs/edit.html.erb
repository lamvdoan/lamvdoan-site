<%= link_to 'Show', @blog %> | <%= link_to 'Delete', @blog, method: :delete, data: { confirm: 'Are you sure?' } %> | <%= link_to 'Back to Blog', blogs_path %>

<h1>Editing Blog</h1>

<%= render 'form', blog: @blog %>
