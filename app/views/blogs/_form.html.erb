<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_field :title, class: "form-control", placeholder: "Title" %>
  </div>

  <div class="form-group">
    <%= form.collection_select(:topic_id, Topic.all, :id, :title, class: "form-control") %>
  </div>

  <div class="form-group">
    <%= form.text_area :description, rows: 5, class: "form-control", placeholder: "Brief description" %>
  </div>

  <div class="form-group">
    <%= form.text_area :body, rows: 15, class: "form-control", placeholder: "Body" %>
  </div>

  <div class="form-group">
    <%= form.submit "Update", class: "btn btn-primary" %>
  </div>
<% end %>
